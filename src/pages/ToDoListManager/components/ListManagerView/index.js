import React from "react";
import TaskList from "../ListManager";

const ToDoListView = () => {
  return (
    <div>
      <h1>To Do list task</h1>
      <div>
        <input type="text" />
      </div>
      <button>Add</button>
    </div>
  );
};

export default ToDoListView;
